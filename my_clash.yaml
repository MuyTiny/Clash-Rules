# ---------------------------------------------------
# MuyTiny 终极模块化云端配置 (2026 增强版)
# 核心升级：增加可视化广告拦截开关 + 隐私追踪保护
# ---------------------------------------------------

# 1. 节点供应
proxy-providers:
  GCP_Nodes:
    type: http
    url: "http://gcp.tiyang.work:2096/sub/my_all_nodes"
    path: ./proxies/my_nodes.yaml
    interval: 3600
    health-check:
      enable: true
      url: http://www.gstatic.com/generate_204
      interval: 300

# 2. 策略组 (增加 🚫 广告拦截 可视化开关)
proxy-groups:
  - name: 🔰 节点选择
    type: select
    use: [GCP_Nodes]

  - name: 💻 程序员专线
    type: select
    proxies: [🔰 节点选择, DIRECT]
    use: [GCP_Nodes]

  - name: 🎮 游戏平台
    type: select
    proxies: [DIRECT, 🔰 节点选择]
    use: [GCP_Nodes]

  - name: 🚫 广告拦截
    type: select
    proxies: [REJECT, DIRECT]

  - name: 🎬 国际媒体
    type: select
    proxies: [🔰 节点选择, DIRECT]
    use: [GCP_Nodes]

  - name: 🐟 漏网之鱼
    type: select
    proxies: [🔰 节点选择, DIRECT]
    use: [GCP_Nodes]

  - name: 🌍 全球直连
    type: select
    proxies: [DIRECT, 🔰 节点选择]

# 3. 规则供应 (增加了隐私追踪库)
rule-providers:
  reject:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/reject.txt"
    path: ./ruleset/reject.yaml
    interval: 86400
  privacy:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/privacy.txt"
    path: ./ruleset/privacy.yaml
    interval: 86400
  icloud:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/icloud.txt"
    path: ./ruleset/icloud.yaml
    interval: 86400
  apple:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/apple.txt"
    path: ./ruleset/apple.yaml
    interval: 86400
  google:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/google.txt"
    path: ./ruleset/google.yaml
    interval: 86400
  github:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/github.txt"
    path: ./ruleset/github.yaml
    interval: 86400
  microsoft:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/microsoft.txt"
    path: ./ruleset/microsoft.yaml
    interval: 86400
  steam:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/steam.txt"
    path: ./ruleset/steam.yaml
    interval: 86400
  proxy:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/proxy.txt"
    path: ./ruleset/proxy.yaml
    interval: 86400
  direct:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/direct.txt"
    path: ./ruleset/direct.yaml
    interval: 86400
  private:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/private.txt"
    path: ./ruleset/private.yaml
    interval: 86400
  gfw:
    type: http
    behavior: domain
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/gfw.txt"
    path: ./ruleset/gfw.yaml
    interval: 86400
  telegramcidr:
    type: http
    behavior: ipcidr
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/telegramcidr.txt"
    path: ./ruleset/telegramcidr.yaml
    interval: 86400
  cncidr:
    type: http
    behavior: ipcidr
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/cncidr.txt"
    path: ./ruleset/cncidr.yaml
    interval: 86400
  lancidr:
    type: http
    behavior: ipcidr
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/lancidr.txt"
    path: ./ruleset/lancidr.yaml
    interval: 86400

# 4. 路由规则
rules:
  # 【优先级 1】自定义强制规则 (精准打击)
  # 网页版和插件的核心 API
  - DOMAIN,translate.googleapis.com,🐟 漏网之鱼
  # 谷歌翻译主站
  - DOMAIN-SUFFIX,translate.google.com,🐟 漏网之鱼
  # 某些地区可能用到的后缀
  - DOMAIN,translate.google.cn,🐟 漏网之鱼
  - DOMAIN-SUFFIX,googleapis.com,🐟 漏网之鱼

  # 【优先级 2】基础连接限制
  - RULE-SET,private,DIRECT
  - RULE-SET,lancidr,DIRECT,no-resolve

  # 【优先级 3】可视化广告/追踪拦截 (映射到策略组)
  - RULE-SET,reject,🚫 广告拦截
  - RULE-SET,privacy,🚫 广告拦截

  # 【优先级 4】细分分流
  - RULE-SET,github,💻 程序员专线
  - RULE-SET,microsoft,💻 程序员专线
  - RULE-SET,steam,🎮 游戏平台
  - RULE-SET,google,🐟 漏网之鱼
  - RULE-SET,apple,🌍 全球直连
  - RULE-SET,icloud,🌍 全球直连
  - RULE-SET,telegramcidr,🐟 漏网之鱼,no-resolve

  # 【优先级 5】代理列表
  - RULE-SET,gfw,🐟 漏网之鱼
  - RULE-SET,proxy,🐟 漏网之鱼

  # 【优先级 6】直连列表
  - RULE-SET,direct,🌍 全球直连
  - RULE-SET,cncidr,🌍 全球直连,no-resolve

  # 【优先级 7】兜底
  - GEOIP,LAN,DIRECT,no-resolve
  - GEOIP,CN,DIRECT,no-resolve
  - MATCH,🐟 漏网之鱼
